#include <string>
#include <iostream>
#include <memory>
#include <vector>

int main() {
  // std::string a = "ccute";
  // std::cout << std::count(a.begin(), a.end(), 'c') << std::endl;
  std::vector<int> a(100);
  std::cout << 
}